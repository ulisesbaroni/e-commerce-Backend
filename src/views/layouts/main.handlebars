<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>E-commerce Camisetas</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <nav class="nav-principal">
    <a href="/">Inicio</a>
    <a href="/products">Productos</a>
    {{#if user}}
      {{#if user.cart}}
        <a href="/carts/{{user.cart}}">Mi carrito</a>
        <a href="/tickets">Mis compras</a>
      {{/if}}
      {{#if isAdmin}}
        <a href="/admin/products">Administración</a>
      {{/if}}
      <a href="#" id="logout-link">Cerrar sesión ({{user.email}})</a>
    {{else}}
      <a href="/login">Ingresar</a>
      <a href="/register">Registrarme</a>
    {{/if}}
  </nav>

  {{{body}}}

  <script src="/js/auth.js"></script>
</body>
</html>